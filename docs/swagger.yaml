definitions:
  v1.PoolDetails:
    properties:
      active_stake:
        type: number
      address:
        type: string
      apr:
        type: number
      avg_score:
        type: integer
      avg_skipped_slots:
        type: number
      delinquent:
        type: number
      depossit_fee:
        type: number
      name:
        type: string
      nodes:
        type: integer
      rewards_fee:
        type: number
      tokens_supply:
        type: number
      unstake_liquidity:
        type: number
      validators:
        items:
          $ref: '#/definitions/v1.Validator'
        type: array
      withdrawal_fee:
        type: number
    type: object
  v1.Validator:
    properties:
      active_stake:
        type: number
      apr:
        type: number
      data_center:
        type: string
      fee:
        type: number
      node_pk:
        type: string
      score:
        type: integer
      skipped_slots:
        type: number
      vote_pk:
        type: string
    type: object
info:
  contact: {}
paths:
  /pool/{name}:
    get:
      consumes:
      - application/json
      description: get pool
      parameters:
      - description: Pool name
        in: path
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/v1.PoolDetails'
      summary: get pool
swagger: "2.0"
